# Project template metadata for Copier
project_name:
  type: str
  help: "Project name"

package_name:
  type: str
  help: "Python package name"
  default: "{{ project_name | lower | replace('-', '_') | replace(' ', '_') }}"
